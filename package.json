{
  "name": "sentechain-miniapp",
  "version": "1.0.0",
  "description": "Web3 Mini-App for sending, receiving, and saving USDT on Celo",
  "scripts": {
    "dev": "concurrently \"npm run dev:frontend\" \"npm run dev:backend\"",
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm run dev",
    "build:frontend": "cd frontend && npm run build",
    "install:all": "npm install && cd frontend && npm install && cd ../backend && npm install",
    "deploy:celo": "npx hardhat run smart_contracts/deploy.js --network celoAlfajores",
    "deploy:celo:mainnet": "npx hardhat run smart_contracts/deploy.js --network celoMainnet",
    "deploy": "npx hardhat run smart_contracts/deploy.js --network celoAlfajores",
    "compile": "npx hardhat compile",
    "test": "npx hardhat test"
  },
  "keywords": [
    "web3",
    "celo",
    "stablecoin",
    "defi",
    "africa"
  ],
  "author": "SenteChain Team",
  "license": "MIT",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^4.0.0",
    "concurrently": "^8.2.2",
    "hardhat": "^2.19.4"
  },
  "dependencies": {
    "@openzeppelin/contracts": "^5.0.1",
    "dotenv": "^16.3.1",
    "ethers": "^6.9.0"
  }
}